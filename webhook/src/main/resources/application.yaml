spring:
  application:
    name: webhook
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: 'jdbc:mysql://localhost:3306/school_data_db'
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
